import React from 'react';

const product = () => (
  <div>
    <h1>this is INDEX a page</h1>
  </div>
);

product.getInitialProps = async () => {
  // console.log('product page: ');
};

export default product;
